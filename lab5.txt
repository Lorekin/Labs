1. Проанализировать приложенные исходные файлы и написать CMakeLists.txt,
осуществляющий сборку соответствующих исполняемых файлов с помощью make(1) или ninja(1).

2. Включить юнит-тесты с помощью enable_testing() (см. cmake-commands(7)) и
добавить хотя бы один корректно отрабатывающий юнит-тест.

